# SSH 服务器训练提示

## ✅ 好消息

训练过程**完全不需要可视化界面**！

- 训练使用 `OffScreenRenderEnv`（离屏渲染）
- 评估在后台运行，不打开任何窗口
- 完全适合 SSH 无头服务器

## 当前配置已经很好

你现在的训练命令已经可以正常运行：

```bash
./train_direct.sh 0 42 bc_rnn_policy base
```

## 如果想加速训练

评估会占用一些时间，如果想加快训练速度：

### 选项 1: 减少评估频率（推荐）

```bash
python libero/lifelong/main.py \
    seed=42 \
    benchmark_name=LIBERO_10 \
    policy=bc_rnn_policy \
    lifelong=base \
    train.num_workers=0 \
    eval.eval_every=10 \
    eval.n_eval=10
```

**效果**：
- 评估时间减少约 50%
- 仍然能选择最佳模型
- 总训练时间从 5-10 小时减少到 4-7 小时

### 选项 2: 最小评估（最快）

```bash
python libero/lifelong/main.py \
    seed=42 \
    benchmark_name=LIBERO_10 \
    policy=bc_rnn_policy \
    lifelong=base \
    train.num_workers=0 \
    eval.eval_every=10 \
    eval.n_eval=5
```

**效果**：
- 评估时间减少约 75%
- 总训练时间约 3-5 小时

### 选项 3: 禁用评估（不推荐）

```bash
python libero/lifelong/main.py \
    seed=42 \
    benchmark_name=LIBERO_10 \
    policy=bc_rnn_policy \
    lifelong=base \
    train.num_workers=0 \
    eval.eval=false
```

**缺点**：
- 无法知道哪个 epoch 的模型最好
- 只能使用最后一个 epoch 的模型

## 时间对比

| 配置 | 评估频率 | 评估次数 | 总时间 |
|------|----------|----------|--------|
| 默认 | 每 5 epoch | 20 次 | 5-10 小时 |
| 推荐 | 每 10 epoch | 10 次 | 4-7 小时 |
| 最快 | 每 10 epoch | 5 次 | 3-5 小时 |
| 无评估 | - | - | 2-4 小时 |

## 推荐配置

对于 SSH 服务器，我推荐使用"推荐"配置：

```bash
python libero/lifelong/main.py \
    seed=42 \
    benchmark_name=LIBERO_10 \
    policy=bc_rnn_policy \
    lifelong=base \
    train.num_workers=0 \
    eval.eval_every=10 \
    eval.n_eval=10
```

## 关于实时显示评估

`evaluate_with_display.py` 中的 `--display` 选项**只在评估时使用**，训练时不会用到。

训练完成后，如果你想看实时显示：
1. 需要 X11 转发：`ssh -X user@server`
2. 或者使用 VNC 连接
3. 或者不使用 `--display` 参数

## 总结

- ✅ 当前训练配置完全适合 SSH 服务器
- ✅ 不需要修改任何东西
- ✅ 如果想加速，可以减少评估频率和次数
- ✅ 训练过程完全无头运行

继续你的训练即可！🚀
